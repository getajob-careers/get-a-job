# Session Handoff — 2026-06-29 — CV System Overhaul (tailoring, renderer, drop-fixes)

## Who / workflow
Eli, founder of Get A Job (getajob.careers). Workflow: Eli scopes in Claude.ai → Claude drafts paste-ready prompts for Claude Code (CC) → CC investigates/builds and HOLDs → Eli reviews/merges. Claude independently verifies all CC claims via Supabase MCP before approving merges/deploys. Conventions: short dense answers; no em dashes anywhere; never trust CC self-reports without DB verification; edge functions need manual deploy (never auto on merge); squash-merge then delete branch as separate steps; destructive DB ops gated on Eli approval; never paste service-role keys into chat.

Repo `getajob-careers/get-a-job`. Supabase ref `ilmqmodklutztuybsvwd`. Main clone `/Users/elienglard/getajob`, worktree `/Users/elienglard/getajob-eval`. Eli's user_id `4b243f3a-5035-474e-a89d-aff13fe06cc2`. Team: Isaac (dev, owns design/templates/landing), Noms (lawyer/wife).

## THE THROUGH-LINE (most important takeaway)
Every CV bug hit today traces to ONE root cause: **the LLM master-author step re-authors structured data it should never touch, and non-deterministically drops/mangles fields.** The deterministic builder (`buildMasterCvData` in `cvDataAdapter.js`) preserves everything faithfully; the LLM author path does not. This caused: dropped professional experience, skills all dumped in one bucket, and missing degree field_of_study. The umbrella fix (NOT yet done) is: **build the master deterministically from structured profile/education rows; use the LLM only to tailor/reframe, never to re-author structured fields.**

## SHIPPED + VERIFIED TODAY (all live on main)

1. **Tailored-CV experience rework (3 stages, complete):** engine returns cv_id + one-tailored-per-app dedup (generate-tailored-cv, was v131); frontend outcome card "Your [role] CV is ready" + View/Download, select-by-application_id, orphans hidden (merged b3dfdab/#434); data cleanup ran directly via execute_sql (deleted 9 dupes + 20 orphans, application_cvs 80→51: 31 masters, 20 tailored-linked, 0 orphans, 0 dupes — VERIFIED).

2. **Speed re-route (#435, 9f1afe6, frontend-only):** studio "Tailor to a job" now calls **refine-cv** (the extension's ~16s select-and-reword path) instead of generate-tailored-cv (~50s full re-author). Body: `{application_id, job_description, cv_model:"sonnet", ops_variant:"v3", grounding:"default"}`. Reads `data.tailoring`. No-app paste requires picking an app (no phantom creation). No-master detection shows one-time "~40s master build" stage. Staged progress replaces blank spinner. Master-is-comprehensive is the intended model (refine selects+reframes from full reservoir).

3. **Density-first renderer (#436, f5e8ed8, DEPLOYED render-cv v4 / generate-tailored-cv v132 / refine-cv v14):** `build-pdf.ts` — deleted ALL cut/curation/floor logic. ONE layout (comfortable) + ONE uniform scale factor → shrinks everything proportionally to fit one page; **never cuts, never errors, never overlaps** (overlap is structurally impossible under uniform scale). `CvFit = {scale, dense, lowestY}`. The alarming "too long" toast is gone; optional calm dismissible hint only when `fit.dense` (scale<0.72). Also in this branch: **education reverse-chronological** (recencyKey — Reichman 2023-Present above Torah Academy 2014-2018) and **skills-by-group rendering** (renders stored {domain,technical,tools} buckets with labels: domain→"Core Competencies", technical→"Technical", tools→"Tools"; empty groups skipped). Eli's CV renders clean at scale ~0.643, everything intact.

4. **Dropped-experience fix (#437, ce1a6d4, DEPLOYED refine-cv v14→v15):** THE serious one. refine-cv's ops prompt said "ONE-PAGE BUDGET: select 3-5 most JD-relevant experiences, drop the rest" and assembleJobCv dropped unselected experiences — the cut-first philosophy we'd removed from the renderer, still alive in the engine. A real JD (app 7093ee79, Workiz PM) made Sonnet drop BOTH Eli's professional roles → prof_exp=0 → CV with no work history. **Fix (Option A):** removed the select field + "drop the rest" instruction; rewrote prompt to "EVERY experience is always kept, you do NOT select/rank/drop, a weak keyword match is a REFRAMING task not a reason to drop, one-page is the renderer's job"; removed both drop points in assembleJobCv; unstamped experiences now carry verbatim. **VERIFIED:** re-tailored the exact failing JD (7093ee79) post-fix → new CV 3706eb55 has prof_exp=2, companies "Guardio | Get a Job". generate-tailored-cv was already safe (its prompt already says "include every experience, NEVER drop entries"). **NOTE: rollback ref is v14 but v14 HAS the drop bug — there is no clean rollback; forward-only.**

Eli's stated principle (drives all tailoring): **"The job isn't to select and say my experience isn't relevant — it's to MAKE my experience relevant."** Tailoring reframes all real experience toward the role; never drops a job for not keyword-matching.

## NOT YET DONE — NEXT PROJECT: generation-quality / match-scorer bundle
Eli explicitly bundles these because they share the LLM-authoring root cause + the skill-resolution pipeline (ESCO taxonomy):
- **Deterministic master build (umbrella fix):** refine-cv's lazy master-build (and any author path) should produce the master from `buildMasterCvData`-equivalent mapping, NOT LLM authoring. Fixes degree-drop AND skills-bucketing together. Engine change, needs deploy.
- **Skills categorization:** LLM dumps ~everything into the `domain` bucket (Eli's CV = 40 domain / 0 technical / 0 tools; many users same; some good like 20/12/17). Renderer already groups correctly + is forward-compatible — fix is upstream at generation. SQL`SELECT (cv_data->'skills'->'domain'...)` distribution confirms widespread.
- **Education field_of_study drop:** LLM master-author drops the major (Eli's master f6b0d4bd stored "Bachelor's Degree" with NO field, though education table row HAS field_of_study="Business Administration - Digital Innovation"). Source data intact for ALL affected users — fully recoverable, just stale LLM-authored snapshots from before the schema hardened (~pre 06-18). NOT data loss, NOT user-trapping (users CAN fix in Profile→Education, EducationTab.jsx:267 always-present input).
- **Backfill stale masters** from education table rows once master is deterministic (Eli's would become "Bachelor's Degree in Business Administration - Digital Innovation"). Do AFTER deterministic fix, not before.
- **Match scorer / ESCO taxonomy** — the senior user's actual complaint (fake-high match scores from domain coverage gaps). This is the anchor of the bundle.

## OTHER PENDING (smaller, tracked)
- **refine-cv dedup follow-up:** refine-cv also blind-inserts (version:1 hardcoded, no dedup) → duplicate application_cvs rows on re-tailor. Frontend list-query dedup (newest-per-app) HIDES it from users, so not user-visible. The one-tailored-per-app engine fix now targets refine-cv (was generate-tailored-cv), needs a deploy.
- **Reframing depth (4-reword cap):** refine-cv rewords ≤4 bullets toward the JD, rest verbatim. Drop-fix works regardless. If tailored output feels too shallow, raise the cap (tradeoff: more output tokens + latency on the ~32s call). Tuning, not a bug.
- **Master "dense" double-download UX:** the dense hint made Eli press Download twice — confusing. Minor.
- **Studio editor field_of_study gap:** studio education editor only shows the field input when already non-empty (CVStudioView ~715, `{ed.field ? … }`). Low priority since users can fix in Profile, but ideally always-show so they can fix where they view the CV.
- **Career-analysis timeout/retry-UX:** one senior user (a9ffb206) hit a 100s timeout / unhandled 500. Function runs near ceiling (~33s avg, 51s max + this 1 fail). No retry UX. High-leverage small fix as senior profiles grow.
- **Stray legacy WIP parked in stash@{0}** (recoverable, NOT Eli's — anti-fab/landing/edit-cv/refine-cv changes; CC stashed it twice to get clean checkouts). Untouched.
- **Optional cvs storage-bucket orphan-PDF cleanup** (harmless unreferenced files from deleted rows).
- **Chrome extension:** reviewer test account elienglard34+cwsreview@gmail.com (aa8ee22f) — last sign-in 06-27 was ELI, not Google. Reviewer has NOT logged in yet; extension still in CWS queue (normal). CWS dashboard is authoritative, not Supabase.

## KEY IDs / VERSIONS (current)
Edge fn ACTIVE: render-cv **v4**, generate-tailored-cv **v132**, refine-cv **v15**. main HEAD progressed …→b3dfdab(#434)→9f1afe6(#435 speed)→f5e8ed8(#436 density)→ce1a6d4(#437 reframe-all). Key files: `supabase/functions/_shared/cv-templates/build-pdf.ts` (uniform scale-to-fit, renderSkills/drawSkillGroup, renderEducation/recencyKey), `supabase/functions/refine-cv/index.ts` (reframe-all assembleJobCv, OPS_SYSTEM_PROMPT, lazy master build), `supabase/functions/generate-tailored-cv/index.ts` (already-safe full author), `src/components/cv-studio/CVStudioLive.jsx` + `CVStudioView.jsx`, `src/lib/cvDataAdapter.js` (buildMasterCvData — the deterministic builder), `src/components/.../EducationTab.jsx`. cv_data.education[] = {degree, field_of_study, institution, dates}; renderer joins degree+field as "{degree} in {field}". skills = object {domain:[], technical:[], tools:[]}.

## IMMEDIATE ELI TODO (no code)
Fix his own degree: Profile→Education, set field_of_study to "Business Administration - Digital Innovation", then rebuild master (cv_data is a snapshot — won't update until rebuild).
