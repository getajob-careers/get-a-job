# Documentation has moved

The project documentation now lives as a handbook under [`docs/`](docs/README.md).

👉 **Start here: [docs/README.md](docs/README.md)**

It's a plain-language handbook for the whole team — what Get A Job is, how it works, the Israeli-market domain, a contained engineering section, how we work, operations, and the decisions behind the build. Every living doc shows an owner and a last-reviewed date, and `npm run docs:check` flags anything that's drifted.

(This file is kept only as a redirect for old links.)
