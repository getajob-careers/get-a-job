#env
.env
.mcp.json
.env.*

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Supabase CLI local cache
supabase/.temp/
supabase/.branches/

# Built Chrome extension package (regenerate via extension/PACKAGING.md)
getajob-extension.zip

# Claude Code agent worktrees (duplicate repo checkouts — never commit)
.claude/worktrees/

# Local design-handoff bundle (kept on disk, not tracked)
GetAJob Design System/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
# Raw CV-template TTF sources — the bytes live committed in *-fonts.ts (base64).
supabase/functions/_shared/cv-templates/fonts/
